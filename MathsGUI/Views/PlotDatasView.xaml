<UserControl x:Class="MathsGUI.Views.PlotDatasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{Binding PlotDatas, Source={StaticResource Locator}}">
    <Grid>
        <StackPanel Margin="10">
            <DockPanel>
                <Button Content="Add plot data" Command="{Binding AddPlotDataButtonHandler}"/>
                <Button Content="Remove plot data" Command="{Binding RemovePlotDataButtonHandler}" HorizontalAlignment="Right"/>
            </DockPanel>

            <ListView ItemsSource="{Binding PlotDatas}" SelectedItem="{Binding SelectedPlotData}" Margin="0,10">                
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <DockPanel Margin ="0,10,0,0">
                            <Label Height="50" Width="Auto" Content="{Binding Name}" DockPanel.Dock="Top"/>
                        </DockPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</UserControl>
